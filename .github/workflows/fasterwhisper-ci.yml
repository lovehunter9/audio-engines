# fasterwhisper CI: triggers only; the build is the shared build-image.yml.
name: fasterwhisper-ci

on:
  # Publishes are deliberate: a v* tag publishes this listing base, or dispatch.
  push:
    tags: ['v*']
  # A PR ref publishes nothing: the run is the contract and smoke tests that gate main.
  pull_request:
    paths:
      - 'wrapper/**'
      - 'tests/**'
      - 'bases/fasterwhisper/**'
      - '.github/workflows/fasterwhisper-ci.yml'
      - '.github/workflows/build-image.yml'
  # Dev: dispatch namespace + image_tag to publish; an empty tag publishes only from main or v*.
  workflow_dispatch:
    inputs:
      namespace:
        description: 'Docker Hub namespace'
        required: false
        default: beclab
      image_tag:
        description: 'Explicit tag for a dev build (empty = only main/v* refs publish)'
        required: false
        default: ''

permissions:
  contents: read

concurrency:
  group: fasterwhisper-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/build-image.yml
    with:
      base: fasterwhisper
      repo: audio-fasterwhisper
      namespace: ${{ inputs.namespace || github.repository_owner }}
      image_tag: ${{ inputs.image_tag || '' }}
    secrets: inherit
