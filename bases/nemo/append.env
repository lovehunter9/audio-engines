# audio-nemo: no Dockerfile on purpose, since the upstream image is too big to unpack (see README).
BASE_IMAGE=docker.io/beclab/nvidia-nemo:26.02
PLATFORM=linux/amd64
# nemo lives in the image's own uv venv, NOT the system python (which has torch but no nemo).
AUDIO_PYTHON=/opt/venv/bin/python
