# audio-pyannote is built by appending the wrapper to its deps image (bases/pyannote/deps.Dockerfile).
# No AUDIO_PYTHON here: the deps image already bakes /usr/local/bin/audio-python from a real probe.
PLATFORM=linux/amd64
