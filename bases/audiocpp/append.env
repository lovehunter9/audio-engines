# Wrapper on deps image (bases/audiocpp/deps.Dockerfile). PLATFORM from CI/Makefile.
# The venv interpreter, not the system one: see the PEP 668 note in deps.Dockerfile.
AUDIO_PYTHON=/opt/venv/bin/python
